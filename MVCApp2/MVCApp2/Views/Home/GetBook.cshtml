@using MVCApp2.Models

@{
    ViewData["Title"] = "GetBook";
}

<div>
    @{
        Book b1 = (Book)ViewData["book"];   
    @if(b1!=null)    
        @b1.AuthorName
    else
    { <strong>Book is Null</strong>}
    }
</div>

